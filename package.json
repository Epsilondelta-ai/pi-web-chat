{
  "name": "pi-web-chat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Trusted pi-web chat and prompt composer plugin with !, /, and @ backend support.",
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate.js",
    "build:backend": "./scripts/build-backends.sh",
    "pretest": "npm run build:backend",
    "prevalidate": "npm run build:backend"
  },
  "keywords": [
    "pi-web",
    "plugin",
    "chat"
  ],
  "license": "UNLICENSED",
  "devDependencies": {}
}

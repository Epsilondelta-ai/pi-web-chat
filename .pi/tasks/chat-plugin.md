# Chat plugin implementation

- Create pi-web plugin manifest, frontend entry, backend script, docs, tests.
- Implement chat/composer enhancement layer: `/`, `!`, `@` popovers and prompt helpers.
- Implement backend methods for slash registry, shell execution, file search/read/resolve-context.
- Verify with automated tests and commit.

# TypeScript chat plugin rewrite

- Compared `pi-web-sidebar` plugin shape and pi-web plugin docs.
- Rewrote pi-web-chat browser entry in TypeScript under `src/`, bundled to `index.js`.
- Added strict typecheck/build/check scripts and DOM activation cleanup test.
- Verification: `npm run check`.
